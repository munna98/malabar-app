import React from 'react';

const Preloader = () => (
  <div className={styles.preloader}>
    <div >
      <img src="/logo.png" alt="Loading..." /> {/* Replace with your logo */}
    </div>
  </div>
);

export default Preloader;
